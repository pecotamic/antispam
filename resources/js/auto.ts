import { setupContactForms } from './contact-form'

/**
 * Entry point for sites that need no configuration — the common case.
 * Sites that do import setupContactForms and call it with options instead.
 */
setupContactForms()
